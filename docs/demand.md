## 需求
这是一个桌面软件。主要功能参考如下：我想实现一个番茄时钟和todolist的功能。基于windows10及以上用户。是一个桌面小组件，可以设置悬浮置顶，可靠边，大小可调整。
整个界面描述如下：上面是一个数字的番茄时钟（12:12:12）下面是一个todolist框。

番茄时钟旁有个开始、暂停、停止的图标。开始和暂停图标互相交替，不会同时出现。
番茄时钟当点击停止或启动软件时，可以设置时钟倒计时，同时图标变为开始。点击开始图标时，开始倒计时直到0
番茄时钟倒计时到0后，全屏提示时间到2秒或点击后渐隐
todolist默认能看到5条记录，可以向下滚动查看更多。
todolist第一行是可直接输入，回车即添加一条。
todolist倒序展示，后创建的在上面。
todolist的左边有个圆形图标，点击表示完成，同时该条记录添加删除线，并移动该条记录到底部，再次点击，移除删除线，恢复到之前创建的状态
todolist右键一条记录，弹出菜单，删除、修改、标记完成图标。
todolist悬浮到一条记录时，该记录高亮


## log
```sh
claude -c
主要使用中文对话。文档和注释也都是中文。

/speckit.constitution 这是一个桌面软件。参考业界通用标准，代码风格，样式要扁平、去瑞锐利。主要功能参考如下，但不在
  章程中体现功能，只是给你用作参考：我想实现一个番茄时钟和todolist的功能。基于windows10及以上用户。是一个桌面小组件，
  可以设置悬浮置顶，大小可调整。

/speckit.specify [Pasted text #1 +10 lines] # 该文档上面的需求

/speckit.clarify 该软件需要设置最小大小。高度最小是时钟加2行todolist，宽度最小是时钟可见。当倒计时结束时，默认按25、
  5分钟这样轮询展示。  边界情况回答：1、保留 2、与一一致，从数据库恢复数据有什么就是什么 3、todolist超过20条则分页
  4、todolist单条不能超过100个中文，显示时，只显示前20个字符（中英），鼠标悬浮展示全部。5、不考虑，就一个。6、继续。7
  、也渐隐退出。

/speckit.plan 开始计划

/speckit.plan 基于现有计划修改：删除todo_tasks表的position字段。位置由is_completed和updated_at排序即可。如：select *
  from todo_tasks order by is_completed desc, updated_at desc.

/speckit.plan 基于现有计划修改：数据库存储和其他信息都在当前用户常用temp文件夹下。 .exe文件无需安装，直接运行。

```